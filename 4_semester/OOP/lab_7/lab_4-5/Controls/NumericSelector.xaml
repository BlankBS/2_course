<UserControl x:Class="lab_4_5.Controls.NumericSelector"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="100">
	<Border Background="#252525" CornerRadius="5" Padding="2" BorderBrush="#F2A900" BorderThickness="1" HorizontalAlignment="Center">
		<StackPanel Orientation="Horizontal">
			<Button Content="-" Click="BtnDown_Click" Width="25" Background="{DynamicResource CardColor}" Foreground="White" BorderThickness="0"/>
            <TextBox x:Name="InputValue" 
                     Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}" 
                     Foreground="#F2A900" 
                     Background="Transparent"
                     BorderThickness="0"
                     Width="40" 
                     TextAlignment="Center" 
                     VerticalAlignment="Center"/>
            <Button Content="+" Click="BtnUp_Click" Width="25" Background="{DynamicResource CardColor}" Foreground="White" BorderThickness="0"/>
		</StackPanel>
	</Border>
</UserControl>