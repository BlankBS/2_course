<UserControl x:Class="lab_4_5.Controls.SkinCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="220">

	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVis" />

		<Style x:Key="CardBorderStyle" TargetType="Border">
			<Setter Property="Background" Value="{DynamicResource CardColor}"/>
			<Setter Property="CornerRadius" Value="10"/>
			<Setter Property="Effect">
				<Setter.Value>
					<DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.5" ShadowDepth="2"/>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<Border Style="{StaticResource CardBorderStyle}" Margin="10" x:Name="MainBorder" RenderTransformOrigin="0.5,0.5">
		<Border.RenderTransform>
			<ScaleTransform ScaleX="1" ScaleY="1"/>
		</Border.RenderTransform>

		<Grid>
			<Path Data="M 0,0 L 40,0 L 0,40 Z" Fill="{DynamicResource AccentColor}" HorizontalAlignment="Left" VerticalAlignment="Top"/>

			<StackPanel>
				<Image Source="{Binding ImagePath}" Height="120" Stretch="Uniform" Margin="0,10"/>

				<TextBlock Text="{Binding ShortName}" Foreground="{DynamicResource AccentColor}" FontWeight="Bold" FontSize="14" TextAlignment="Center"/>
				<TextBlock Text="{Binding Rarity}" Foreground="#F2A900" FontSize="10" TextAlignment="Center" Margin="0,2"/>

				<TextBlock Foreground="LightGray" FontSize="11" TextAlignment="Center" Margin="0,5">
					<Run Text="{DynamicResource m_InStock}"/>
					<Run Text="{Binding Quantity, Mode=OneWay}"/>
					<Run Text="{DynamicResource m_Unit}"/>
				</TextBlock>

				<TextBlock Text="{Binding Price, StringFormat={}{0}$}" Foreground="White" FontWeight="Bold" FontSize="18" TextAlignment="Center" Margin="0,5"/>

				<Button Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="{Binding}"
                        Height="35" Margin="5">
					<Button.Style>
						<Style TargetType="Button" BasedOn="{StaticResource PubgButtonStyle}">
							<Setter Property="Content" Value="{DynamicResource m_AddToBasket}"/>
							<Style.Triggers>
								<DataTrigger Binding="{Binding Quantity}" Value="0">
									<Setter Property="IsEnabled" Value="False"/>
									<Setter Property="Opacity" Value="0.5"/>
									<Setter Property="Content" Value="{DynamicResource m_OutOfStock}"/>
									<Setter Property="Foreground" Value="{DynamicResource MainText}"/>
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</Button.Style>
				</Button>

				<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
					<Button Content="⚙"
                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}"
                            Visibility="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}"
                            Width="40" Margin="5" Background="Transparent" Foreground="White"/>

					<Button Content="❌"
                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}"
                            Visibility="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}"
                            Width="40" Margin="5" Background="Transparent" Foreground="Red" BorderBrush="Red"/>
				</StackPanel>
			</StackPanel>
		</Grid>

		<Border.Triggers>
			<EventTrigger RoutedEvent="MouseEnter">
				<BeginStoryboard>
					<Storyboard>
						<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.2"/>
						<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.2"/>
					</Storyboard>
				</BeginStoryboard>
			</EventTrigger>
			<EventTrigger RoutedEvent="MouseLeave">
				<BeginStoryboard>
					<Storyboard>
						<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
						<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
					</Storyboard>
				</BeginStoryboard>
			</EventTrigger>
		</Border.Triggers>
	</Border>
</UserControl>